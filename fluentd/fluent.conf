<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type copy
  <store>
    @type stdout
  </store>
  <store>
    @type forward
    <server>
      host otel-collector
      port 8006
    </server>
  </store>
</match>
